server:
  port: 8001

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/nevlate/springcloud_demo
          search-paths:                # git仓库地址下的相对地址，可以配置多个，用,分割。
          username: nevlate  #账号密码写真实的快一些我觉得，不使用也能访问有点慢
          password: zhm1211